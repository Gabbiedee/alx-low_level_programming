#include "main.h"

/**
 * _puts - prints string
 * @str: input string to print
 * Description: prints string to stdout
 *
 * Return: nothing
 */
void _puts(char *str)
{
	int dol = 0;

	while (*s != '\0')
	{

		dol++;
		s++;
	}

	return (dol);
}
